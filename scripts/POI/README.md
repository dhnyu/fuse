# POI Scripts

This directory contains OSM point-of-interest extraction workflows.

`10_extract_seoul_osm_pois.R` extracts Seoul POI features from OSM inputs and writes canonical spatial/tabular outputs. POI extraction is related contextual data preparation, not part of the core Street View acceptance pipeline.

The numbering indicates preprocessing order within the POI workflow.
