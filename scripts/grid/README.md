# Grid Scripts

This directory contains early-stage grid preprocessing scripts.

`10_build_seoul_grid_500m.R` builds the reusable 500 m Seoul grid from the configured Seoul boundary. Its outputs are consumed by road-network sampling, diagnostics, and map rendering.

The numbering marks this as a prerequisite preprocessing step rather than a Street View acquisition step.
