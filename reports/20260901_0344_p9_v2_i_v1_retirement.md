# P9 v2 V2-I V1 Retirement

## Verdict

`P9_V2_I_V1_RETIRED_PASS_PUSHED` is the intended final verdict, conditional on
the final commit, push, clean-tree/origin check, and post-push immutable
readback recorded in the final response.

## Purpose and lineage

- Executed: 2026-09-01 03:44 Asia/Seoul.
- Fuse start: `reduced@3e9931fd7c428500d5488fc026ceed1af6ffd7f1`,
  clean and origin ahead/behind `0/0`.
- Dissertation:
  `reduced@ad8c8b5c17c5ca72dce7f30c2eb283f6041dbc9a`, clean,
  origin ahead/behind `0/0`, and unchanged.
- Input prompt: retire P9 v1 execution and downstream resolution, preserve all
  legacy evidence read-only, and leave canonical V2 acceptance unchanged.

## Retirement implementation

The stable error is `P9_V1_EXECUTION_RETIRED`. It states that v1 is
historical/read-only, execution is retired, and callers must use a canonical
`p9accv2_*` identity through `resolve_accepted_checkpoint()`.

The active main, isolated-formal, and recovery target graphs each contain one
retirement guard and no executable v1 closure:

| Script | Sole active P9 target |
|---|---|
| `_targets.R` | `p9_v1_main_execution_retired` |
| `_targets_p9_formal.R` | `p9_v1_formal_execution_retired` |
| `_targets_p9_recovery.R` | `p9_v1_recovery_execution_retired` |

Eight command-line entry scripts reject before argument parsing. Their imported
main functions also reject, as do direct cache-build, recovery-authority
publication, recovery execution, the recovery controller constructor, and the
old `resolve_committed()` downstream resolver. The old target declarations and
implementation bodies remain under historical-only names for source audit;
there is no redirect or compatibility launch into V2.

Read-only inspection remains available through `inspect_retirement_sources()`,
`inspect_legacy_run()`, `validate_legacy_import()`, checkpoint-pair audit,
identity diagnostics, and `inspect_committed_recovery()`.

## Retirement manifest

- Identity: `p9ret_7921290e923f5d879e6d84c1`.
- Content SHA-256:
  `7921290e923f5d879e6d84c18127c990593bf71d39b67fd6dd73c583a47d2749`.
- Canonical path:
  `/mnt/hdd002/dhnyu/fusedata/models/reduced/p9_v2/canonical/retirement/p9ret_7921290e923f5d879e6d84c1/retirement_manifest.json`.
- Publication basis: explicit V2-I user work unit; no new execution authority.
- Publication: canonical JSON, schema validated, same-filesystem staging,
  file `fsync`, atomic directory rename, directory `fsync`, and
  create-or-validate collision handling.
- Duplicate publication: returned the same path and bytes; exactly one
  `p9ret_*` manifest exists.

The manifest binds all retirement implementation source hashes, the replacement
acceptance/resolver, 9 formal authorities, 3 recovery authorities, and all six
historical stores. Every authority is `RETIRED_INELIGIBLE`; every store is
`HISTORICAL_READ_ONLY`. No authority or store content was edited to apply that
classification.

## Preserved evidence

The formal authority inventory contains 9 identities and the recovery inventory
contains 3 identities, matching the complete historical set. The store inventory
contains:

1. `fuse-p9-formal`
2. `fuse-p9-formal-p9gen_acb72f05336e09451b4ac458`
3. `fuse-p9-formal-p9gen_batchuniq_20260831`
4. `fuse-p9-recovery-20260831`
5. `fuse-p9-recovery-complete-20260831`
6. `fuse-p9-recovery-lockstate-20260831`

Each manifest entry records its deterministic ordered tree-inventory hash and
entry count. Existing reports, checkpoints, validation records, state, locks,
owners, heartbeats, and target metadata remain inspection-only evidence.

## Downstream enforcement

P9-B, selected-FM, held-out evaluation, P10, and P11 all resolved canonical
acceptance `p9accv2_d93b01ef13c3f26a22287ce7` to the identical checkpoint
`p9ck_42f7957d2ea998ac9e8ff705`. The 5-by-8 rejection matrix rejected v1
checkpoint IDs, v1 authority IDs, v1 recovery IDs, direct store paths, manual
paths, `latest`, direct bundle paths, and direct finalization paths. No fallback
exists and no downstream scientific task ran.

The verified canonical chain remains authority -> imported ledger -> bundle ->
finalization -> acceptance -> eligibility -> resolver. Excluding the newly
authorized retirement namespace, its 239-file content inventory stayed exactly
`def55be73df1faba7299c3fd11d6e948d5a5ce7a8a993629f98c57f736bbbd3c`.

## Validation

- Focused V2-I Python: 56 passed, 0 failed.
- Combined V2-A/B/C/D/E/F/G plus V2-I: 374 passed, 0 failed.
- Existing relevant v1 P9 Python: 64 passed, 0 failed, 58 skipped because the
  two recovery execution/crash suites test the now-retired active contract.
- Relevant R testthat: 46 passed; only the two documented pre-existing stale
  generation assertions failed.
- New R retirement assertions: 7 passed.
- Main/formal/recovery `targets::tar_validate()`: 3/3 passed; no target ran.
- Target manifests: 3/3 contain exactly one retirement target.
- Dependency network HTML regenerated from the retired main graph.
- Python AST parsing: 12 files passed; R parsing: 10 files passed.
- Retirement schema: JSON parsed and Draft 2020-12 meta-validation passed.
- Canonical manifest schema validation/readback and idempotent publication:
  passed.
- `git diff --check`: passed before report publication.

## Complexity accounting

V2-I adds one cohesive 211-line retirement module, one stable exception class,
and seven functions: two guards, two hash/inventory helpers, one read-only
inventory API, one manifest builder, and one atomic publisher. It adds one
bounded runtime schema and one small R guard. It creates no controller,
authority hierarchy, reservation, attempt, operation, recovery path, mutable
retirement state, lock class, or fallback resolver.

Active v1 P9 target closure falls from the historical main/formal/recovery
graphs to 1/1/1 fail-closed targets. The six stores remain historical and no
new target store is created.

## Prohibited-work accounting

| Activity | Count |
|---|---:|
| Training / resume / recovery | 0 / 0 / 0 |
| Validation / held-out evaluation / metric recomputation | 0 / 0 / 0 |
| Checkpoint/cache writes | 0 / 0 |
| Historical evidence mutation | 0 |
| Dissertation mutation | 0 |
| New v1 authority/reservation/attempt/operation | 0 / 0 / 0 / 0 |
| New canonical historical import/finalization/acceptance | 0 / 0 / 0 |
| Downstream scientific execution | 0 |
| V2-H implementation | 0 |
| New V2-I retirement manifest | 1 (explicitly required) |

## Immutable pre/post comparison

| Evidence | Before | After |
|---|---|---|
| Epoch-105 payload | `fdac720bff12fb25747f69977bc69c5fb28adbc4a73cabd879b1b57c47cc06b6` | same |
| Epoch-105 manifest | `87010ce01ad74fc7b2652bf0e5a7e56baea2bef88400f6f23748d1e39f9227bc` | same |
| Full v1 source inventory | `282e8eb48ac5ae9efeabb5d535707e1e9273d3bfd803ec3b8c03f9b74063065c` | same, 58 entries |
| Existing canonical V2 inventory | `def55be73df1faba7299c3fd11d6e948d5a5ce7a8a993629f98c57f736bbbd3c` | same, 239 files excluding retirement |
| V1 terminal state | `FAILED_NONRESUMABLE` | same |

## Files and risks

Runtime changes are limited to the retirement module/schema, guards at existing
v1 boundaries, the renamed read-only recovery inspector, and target-list
selection. Tests cover Python/R guards, source inventories, manifest
publication, resolver rejection, and canonical readback. Blueprint status,
roadmap, migration, inventory, decisions, and risk R14 were updated, and the
dependency network was regenerated.

The two stale R generation expectations remain documented and unrelated to
retirement behavior. Historical code is intentionally retained, so source-level
functions remain readable; active target, CLI, controller, and resolver
boundaries prevent their execution.

## Next work unit

V2-H is optional and only required before any future new P9 training run:

`V2-H: production v2 training-controller implementation and production-shaped non-training pilot for future runs.`

V2-H was not started.
