# S10 viewer fixed port 8765

## 1. FINAL STATUS

PASS. Created 2026-09-18T04:13:42.991876+09:00 (Asia/Seoul). Input Git commit
`7cb443bfc0d30c8b2f739b9f5f75e10cdb7def49`, branch `reduced`.
User request: retain the accepted 100-query viewer, stop verified temporary S10
servers, fix all serving/validation to 127.0.0.1:8765, validate real production
bytes/DOM, document stale-document handling, commit and push workflow changes.

## 2. Current listeners

Initial `ss -ltnp` showed no 8765 listener and one S10 HTTP listener on 18765.
The only current Python http.server process is PID **1868328**, bound to
127.0.0.1:8765. `/proc/1868328/cmdline` contains the exact command below;
`/proc/1868328/cwd` is `/members/dhnyu/fuse`. Other listeners were not stopped.
The server is retained in tmux session `s10-viewer` (started 04:10:15 KST).
Use `tmux attach -t s10-viewer` and Ctrl+C to stop it when desired.
Complete final listener evidence: `/mnt/hdd002/dhnyu/fusedata/tmp/fuse/s10-fixed-port-20260918_0408/listeners.txt`.

## 3. Old temporary server cleanup

Re-read `/proc/1843525/cmdline` and cwd before sending SIGTERM. This process was
`python -u -m http.server 18765 --bind 127.0.0.1 --directory /mnt/hdd002/dhnyu/fusedata/retrieval_data/reduced/s10_viewers/viewer_eb2cd9af73816e89b1390ded`,
cwd `/members/dhnyu/fuse`. Only this verified S10 process was terminated.
A subsequent `ss -ltnp 'sport = :18765'` returned no listener.
No alternative port was used for this task. No other S10 HTTP server was found.

## 4. Canonical viewer root

`/mnt/hdd002/dhnyu/fusedata/retrieval_data/reduced/s10_viewers/viewer_eb2cd9af73816e89b1390ded`

Acceptance: `s10_acceptance_e41bb7c33d2ae4171a1f2c59`.
Query manifest: `s10_queries_d8875a530a769040d5ec7fb3`.
Existing production bytes were reused unchanged: 100 query JSON files and
100 query HTML files. Viewer receipt SHA256 remains
`8b9649325f48e0e042e315e283d36848d898a7455fb1dbe72bacaeb1ec48fd46`.
The validator checked every file hash declared in this existing receipt.

## 5. Exact command and access URL

```sh
python -u -m http.server 8765 \
  --bind 127.0.0.1 \
  --directory /mnt/hdd002/dhnyu/fusedata/retrieval_data/reduced/s10_viewers/viewer_eb2cd9af73816e89b1390ded
```

Always use **http://127.0.0.1:8765/?model=main&mode=standard**.
New helper `scripts/serve_s10_viewer.py` starts that fixed server in foreground
or reports/verifies the same viewer already running. `--check` is read-only.
An old or unrelated listener fails closed with PID/command information; there
is no PID killing or port fallback. Stop an old S10 server only after explicit
inspection; unrelated listeners block serving. Same-viewer reuse was exercised
against PID 1868328 without restarting it.

## 6. HTTP counts and bindings

Actual GET http://127.0.0.1:8765/config.json: **100 queries, 28 models**.
Acceptance matches the expected 100-query acceptance above.
Last scene: `scn_8d90cc0a32f275618d6927a1`.
Filesystem and HTTP response were compared byte-for-byte and have SHA256:
`01b4761f95e5be8ba526985105f3dd54c6f2922795d6ba061b1a05de61e7772c`.
Evidence: `/mnt/hdd002/dhnyu/fusedata/tmp/fuse/s10-fixed-port-20260918_0408/server.json`.

## 7. DOM and browser regression

Fresh Chromium context, service workers blocked/bypassed, CDP cache disabled.
Entry URL: `http://127.0.0.1:8765/?model=main&mode=standard`. Validator attached to the running server and did not
create a server. Actual DOM: **100 query options, 28 model options**.
Loaded document config SHA matched the HTTP/filesystem SHA above.

| Query | Exact DOM option |
| --- | --- |
| 1 | `1 / 100 · scn_28d2cb1e58601426259844e3` |
| 30 | `30 / 100 · scn_d98bae7d9aad582ad675c412` |
| 31 | `31 / 100 · scn_393afbf52ad5f9afc351d52e` |
| 50 | `50 / 100 · scn_a1572fa0cee3813779f245ab` |
| 100 | `100 / 100 · scn_8d90cc0a32f275618d6927a1` |

Queries 1/30/31/50/100 passed both direct-page loads and dropdown switching.
40 combinations: those five queries × main/ofat_d_256/cmp_DS/cmp_B9 ×
Standard/Non-local. All 28 models' stored query/mode band records were checked;
interactive model coverage was the four representative models, not all 28.
120 thumbnail clicks checked Top/Middle/Bottom; Rank1 checked in every case.
Location names/coordinates, vector layers, LC/DEM image decoding and toggles,
25 thematic maps, detailed summaries, provenance, zoom/reset and responsive
widths 1800/1600/1200 passed. JavaScript errors: zero. Screenshots of all five
queries saved; query 31 screenshot also visually inspected.
173,600 pre-existing band rows were read and checked, not recomputed.

Exact per-case URLs (all 8765) and results:
`/mnt/hdd002/dhnyu/fusedata/tmp/fuse/s10-fixed-port-20260918_0408/validation.json`.
Screenshots: `/mnt/hdd002/dhnyu/fusedata/tmp/fuse/s10-fixed-port-20260918_0408/query_{1,30,31,50,100}.png`.
Console log: `logs/20260918_0408_s10_viewer_fixed_port_8765.log`.

## 8. Stale-document handling

Prior user evidence proved DOM count 30 while a same-origin no-store fetch
returned 100. The document retained old config SHA
`ef19348e347b6ef2c3940b9a191a9672944301d21aff1018e38b02059a941bdc`.
This demonstrates stale loaded document state, not a current 30-query server
response; it does not distinguish an unrefreshed tab from a particular cache
mechanism. Open a new tab at the canonical URL or Ctrl+Shift+R; if needed enable
DevTools Network Disable cache and reload. Never change ports as a workaround.
Remote SSH should forward local8765 to remote8765; Windows local bind problems
do not authorize changing the remote port.

## 9. Documentation, helper and validation changes

- Supplemental README now leads with current 100-query root, exact command/URL,
  tmux usage, conflict inspection, stale-tab handling and Remote SSH policy.
  Earlier publication recipes are explicitly labeled historical.
- Current query revision validator attaches only to verified production8765,
  checks HTTP/DOM config identity, records URL/PID/root/hash evidence and expands
  test queries to include 30 and31.
- Three legacy validators' ephemeral binds changed to fixed8765, so port
  conflicts fail instead of silently selecting a random port. These historical
  scientific/display validators were not executed; use the current validator.
- Added minimal fixed-root serving/check helper and conflict/binding tests.
- Historical audit reports were preserved, including their past diagnostic-port
  evidence. No current source instruction using18765/28765 was found.

Validation: Python syntax checks passed for all five changed Python scripts;
`pytest -q tests/python/test_s10_viewer_serving.py`: **5 passed**.
`git diff --check`: PASS. AST audit confirmed every legacy S10 viewer HTTPServer
bind is127.0.0.1:8765; the current validator has no server bind.
No target definition/dependency/resource/output changed, so targets execution,
`tar_validate` and network regeneration were not applicable and were not run.
No outstanding validation failure. User's existing tab is not remotely reloaded;
it requires the new-tab/reload instruction above.

## 10. Scientific safety

Reviewed latest dissertation retrieval section and current blueprint before
implementation. The historical paper10-query / blueprint30-query policies and
previously approved separate100-query contract remain unchanged; this task only
changes serving/validation workflow. No sampling, ranking computation, embedding
loading/recomputation, checkpoint loading, inference, scientific generation or
viewer content regeneration was run. S09/S11 were not executed or modified.
Existing scientific acceptance, query manifest, rankings, parent viewers and
production viewer files are untouched. Evidence/logs/screenshots remain outside
Git; only workflow source, tests, README and this report are committed.
